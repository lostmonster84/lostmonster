# Stack -- Evolution Log

> Per-project learning for the /stack skill. Never synced upstream.

### Learned Rules

- `/gaffer` SKILL.md has a project-specific design guide path (e.g. `website/.ai/LOST-MONSTER-DESIGN-SYSTEM.md`). Only sync methodology changes, not the path. The Stack version uses `docs/DESIGN-GUIDE.md` as the generic default.

---

## Sync #1 -- 2026-04-04

- **Synced:** /firm (project.json reverse source), /go (auto-sync + setup check)
- **Skipped:** /gaffer (only diff was project-specific design guide path)
- **Generalisation issues:** None -- both changes were already framework-level
- **Missed:** Nothing
- **Friction:** None -- clean sync
