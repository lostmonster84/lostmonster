// Empty loading state for contact page
// This prevents the global loading spinner from showing during navigation
// The morphing button overlay provides the visual transition instead
export default function Loading() {
  return null;
}
